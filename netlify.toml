[build]
  command = "npm run build"
  publish = "dist/client"

# SPA fallback — all routes serve index.html so TanStack Router handles them client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
